const MYSQL = "mysql";
const MARIADB = "mariadb";
const POSTGRES = "postgres";
const MSSQL = "mssql";

//routes
const STATE_NOT_FOUND = 'State not found';

module.exports = {
    MYSQL,
    MARIADB,
    POSTGRES,
    MSSQL,
    STATE_NOT_FOUND,
}